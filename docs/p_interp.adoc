= PILE Base: pInterp
:source-highlighter: rouge
:stylesheet: asciidoctor.css
:linkcss:
:toc: macro
:toclevels: 5

include::snippets/header.adoc[]


__pInterp__ interpolates strings with position-independent arguments.

Dependencies: None.

[source,lua]
----
local pInterp = require("p_interp")

print(pInterp("The $1 brown fox $3 over the lazy $2.", "quick", "dog", "jumps"))

-->The quick brown fox jumps over the lazy dog.
----


toc::[]


== API


=== pInterp

Interpolates up to nine arguments in a string.

[source,lua]
----
local interpolated = pInterp(s, ...)
----

* `s`: The string to inject values into. The insertion points for arguments are `$1` for the first, `$2` for the second, and so on, up to `$9`.

* `...`: A vararg list. All values are converted to strings.

**Returns:** The interpolated string.


[discrete]
==== Notes

The character `$` can be escaped in `s` by writing `$$`.

If you need to interpolate more than `$1` - `$9`, consider breaking the message into smaller strings, or use https://www.lua.org/manual/5.1/manual.html#pdf-string.gsub[**string.gsub**] directly with a replacement table.


include::snippets/footer.adoc[]
